const addresses = {
  // Sustituye por la dirección REAL que te dio:
  // npx hardhat run scripts/deployIpfsStorage.js --network localhost
  ipfs: "0x.................",
};

export default addresses;
